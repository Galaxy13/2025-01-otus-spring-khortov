spring:
  shell:
    interactive:
      enabled: false
  data:
    mongodb:
      database: testdb

mongock:
  enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.11
logging:
  level:
    de:
      flapdoodle:
        embed:
          mongo:
            WARN

